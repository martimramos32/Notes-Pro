<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_marginVertical="8dp"
    android:backgroundTint="@color/white"
    android:background="@drawable/rounded_corner">

    <!-- imagem da nota (thumbnail) -->
    <ImageView
        android:id="@+id/note_image_view"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_marginBottom="8dp"
        android:adjustViewBounds="true"
        android:contentDescription="@string/image_preview"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <!-- linha com título e ícone de pin -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- título da nota -->
        <TextView
            android:id="@+id/note_title_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toStartOf="@+id/pin_icon_view"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/black"/>

        <!-- ícone de pin (visível apenas quando fixada) -->
        <ImageView
            android:id="@+id/pin_icon_view"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_push_pin_filled"
            android:contentDescription="@string/pin_note"
            android:visibility="gone"
            app:tint="@color/my_primary"/>

    </RelativeLayout>

    <!-- conteúdo da nota -->
    <TextView
        android:id="@+id/note_content_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginTop="4dp"/>

    <!-- linha inferior: data + ícone de lembrete -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp">

        <!-- data da nota formatada como MM/DD/YYYY -->
        <TextView
            android:id="@+id/note_date_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/black"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"/>

        <!-- ícone de lembrete (sino) — visível quando tem lembrete agendado -->
        <ImageView
            android:id="@+id/reminder_icon_view"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_notifications"
            android:contentDescription="@string/set_reminder"
            android:visibility="gone"
            app:tint="@color/my_primary"/>

    </RelativeLayout>

</LinearLayout>